export const crawBiWiki = async () => {
	// TODO: List all commands, load their proper casing, load their arguments, load their return types
	// TODO: List all functions, load their proper casing, load their arguments, load their return types
}
